= simple_form_for attachment, remote: true, url: resource_url(attachment), html: {class: 'attachment_upload'} do |f|
  = f.input_field :file, as: :file
  = f.input_field :kind, as: :hidden
  = f.submit 'Отправить'
