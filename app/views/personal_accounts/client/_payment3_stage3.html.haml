#payment3_stage3.rounded-corners.border_x1.shadow_x1
  .close_fly_window
  .head_account_grow
  .line
    На Вашем лицевом счете: #{@amount} руб.
  .line
    Разработка #{@order.title}: #{@order.price} руб.
  .button
    = link_to 'Перейти к оплате услуги', payment2_stage1_personal_account_path(:order => @order.id, :format => :json), :class => 'ajax'

