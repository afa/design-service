%li
  .heading
    = @data.name
  %ul.images#first
    - @data.get_photo_test_answers.each do |answer|
      = render_cell :question_field, "#{answer.display}", :data => answer
  %input{:type => 'hidden', :value => '0', :name => "question_field[photo-#{@data.id}]"}