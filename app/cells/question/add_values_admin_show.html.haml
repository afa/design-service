%div{:style => 'font-size: 20px; margin: 21px 0 10px 10px;'}
  = @question_data.value
%table{:width => '100%', :border => '0', :cellspacing => '0', :cellpadding => '0'}
  %tr
    %th Номер
    %th Текст

  - @data_fields.each_with_index do |value, index|
    %tr
      %td #{(index + 1)}
      %td 
        = value.value